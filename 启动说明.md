# 个人网站启动说明

## 环境要求

- Node.js 18 或更高版本
- npm

## 第一次启动

在 PowerShell 中进入项目目录：

```powershell
cd "D:\ai_project2\个人网站"
```

安装依赖：

```powershell
npm install
```

## 启动开发网站

运行：

```powershell
npm run dev
```

终端会显示本地地址，通常是：

```text
http://127.0.0.1:5173/
```

复制这个地址到浏览器打开即可。修改 `src` 目录中的代码后，页面会自动刷新。

停止网站：在运行命令的终端按 `Ctrl + C`。

## 构建生产版本

检查项目能否正常构建：

```powershell
npm run build
```

构建产物会生成到 `dist` 目录。

## 预览生产版本

先构建，再运行：

```powershell
npm run build
npm run preview
```

然后打开终端显示的地址，通常是：

```text
http://127.0.0.1:4173/
```

## 常见问题

### 5173 端口被占用

关闭占用该端口的旧开发服务，或直接使用 Vite 输出的其他端口地址。可以在 PowerShell 中查看占用进程：

```powershell
Get-NetTCPConnection -LocalPort 5173 -ErrorAction SilentlyContinue
```

### 依赖安装异常

删除 `node_modules` 后重新安装：

```powershell
Remove-Item -Recurse -Force .\node_modules
npm install
```

### 页面没有更新

确认浏览器访问的是当前 Vite 输出的地址，并尝试强制刷新：

```text
Ctrl + F5
```

